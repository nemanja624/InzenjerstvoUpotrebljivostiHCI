<Window x:Class="Mercedes_Models_CMS.Pages.DataPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mercedes_Models_CMS.Pages"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:notifications="clr-namespace:Notification.Wpf.Controls;assembly=Notification.Wpf"
        mc:Ignorable="d"
        Title="DataPage"
        Height="750"
        Width="700"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseDown"
        Background="Transparent">

    <Border CornerRadius="25"
            Background="{StaticResource PrimaryBlackBrush}"
            BorderBrush="{StaticResource AccentGoldBrush}"
            BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <notifications:NotificationArea
                x:Name="DataNotificationArea"
                Panel.ZIndex="1000"
                MaxItems="3"
                Position="TopRight"
                Grid.Row="0"
                Grid.RowSpan="3"
                />

            <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0 30 0 0">
                <TextBlock Margin="10 0 0 0"
               VerticalAlignment="Top"
               FontSize="24"
               Style="{StaticResource TitleText}"
               Text="Dashboard"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        Grid.Row="1"
                        HorizontalAlignment="Right">
                <Button x:Name="Add_Button" Click="Add_Button_Click">
                        <StackPanel Orientation="Horizontal">
                        
                        <fa5:SvgAwesome Icon="Solid_CarAlt" Height="20" Width="20"/>
                        <TextBlock Text="ADD" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>
                </Button>
                <Button x:Name="Delete_Button" Content="DELETE" Click="Delete_Button_Click"/>
                <Button x:Name="Logout_Button" Click="Logout_Button_Click" Content="LOGOUT"/>
            </StackPanel>

            <Border Grid.Row="2"
                    ClipToBounds="True"
                    BorderBrush="{StaticResource AccentGoldBrush}"
                    BorderThickness="1"
                    Margin="5"
                    CornerRadius="10">
                <DataGrid ItemsSource="{Binding CarModels}"
                          x:Name="DataGridCarModels"
                          AutoGenerateColumns="False"   
                          SelectionMode="Extended"
                          IsReadOnly="True"
                          HorizontalScrollBarVisibility="Hidden"
                          Background="Transparent"
                          GridLinesVisibility="Vertical"
                          HorizontalGridLinesBrush="Gray"
                          VerticalGridLinesBrush="{StaticResource AccentGoldBrush}"
                          Foreground="White"
                          Focusable="False"
                          SelectionUnit="FullRow"
                          BorderBrush="{x:Null}"
                          RowHeaderWidth="0"
                          HeadersVisibility="Column">
                    <DataGrid.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#333333"/>
                    </DataGrid.Resources>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="FontWeight" Value="DemiBold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridRow">
                                        <Border x:Name="RowBorder"
                                                CornerRadius="10"
                                                Margin="2"
                                                BorderBrush="{StaticResource AccentGoldBrush}"
                                                BorderThickness="1"
                                                Background="{TemplateBinding Background}">
                                            <DataGridCellsPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentGoldBrush}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn x:Name="dataGridColumnDelete">
                            <DataGridTemplateColumn.Header>
                                <CheckBox
                                    x:Name="checkBoxSelectAll"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Bottom"
                                    Content="Select All"
                                    FontSize="12"
                                    Foreground="{StaticResource AccentGoldBrush}"
                                    Style="{StaticResource DataCheckBox}"
                                    Click="checkBoxSelectAll_Click"
                                    Margin="10 0 0 0"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox
                                    x:Name="chckBox"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                        Click="CheckBoxRow_Click"
                                    IsChecked="{Binding IsSelected}" 
                                    Style="{StaticResource DataCheckBox}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Model Name" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       TextAlignment="Center"
                                       FontSize="15">
                                <Hyperlink Click="Hyperlink_Click"
                                           Foreground="{StaticResource AccentGoldBrush}">
                                    <Run Text="{Binding Name}"/>
                                </Hyperlink>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Image" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="190" Height="150" ClipToBounds="True">
                                        <Image Source="{Binding DisplayImage}"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Stretch"
                                               VerticalAlignment="Stretch"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Added"
                                            Binding="{Binding DateAdded, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                            Width="2*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>
